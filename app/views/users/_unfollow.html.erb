<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id), html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "UNFOLLOW", class: "follow-button profile-button" %>
  <div class ="relationship-button-border button-border">UNFOLLOW</div>
<% end %>
