<% @user ||= current_user %>
<div class="stats">
  <p>followings
    <%= link_to followings_user_path(@user), id: "following" do %>

        <%= @user.followings.count %>

    <% end %>
  </p>

  <p>followers
  <%= link_to followers_user_path(@user), id: "followers" do %>

      <%= @user.followers.count %>

  <% end %>
  </p>

</div>
